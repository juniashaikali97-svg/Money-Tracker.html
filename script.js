// Simple function to handle button clicks
document.querySelectorAll('button').forEach(button => {
    button.addEventListener('click', () => {
        alert("Thank you for your interest! Our application form will open shortly.");
    });
});

// Log a message to the console to confirm the script is working
console.log("Cashnet Finance website is ready!");